version: 2.1 # using 2.1 provides access to orbs and other features
jobs:
  install-node:
    docker: 
      - image: circleci/node:6.16.0
    steps:
      - checkout
      - run: npm install
workflows:
  tester:
    jobs:
      - install-node